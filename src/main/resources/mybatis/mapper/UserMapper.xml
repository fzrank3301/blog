<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.wang.blog.dao.UserMapper">

    <insert id="addUser">
        insert into
        user(id, username, password, email, phonenumber, UUID, banned, createtime)
        value (id,#{name},#{passwd},#{email},#{phonenum},#{uuid},#{banned},#{createtime})
    </insert>

</mapper>